import 'package:flutter/material.dart';

class LearningAnimation extends StatelessWidget {
  Widget build(BuildContext context) {
    return Container(
        margin: EdgeInsets.all(20.0),
        child: Text("Learning animations in flutter..."));
  }
}
